# 146 — Logic Scheme

> Формат — см. [`../documents-composition.md`](../documents-composition.md#146--logic-scheme-146logic-schememd).
> Детализация компонентов — [150](150.component-diagram.md).

```mermaid
flowchart TB
    UI["Веб-клиент (React)"] -->|REST + токен| API["API-слой (Spring Boot)"]

    subgraph Domain["Прикладные сервисы"]
        WI["Модуль рабочих элементов"]
        IT["Модуль итераций и ёмкости"]
        COL["Модуль совместной работы: комментарии, вложения"]
        AUD["Модуль аудита"]
        NOT["Модуль уведомлений"]
        INT["Интеграционный модуль"]
    end

    API --> WI
    API --> IT
    API --> COL
    API --> NOT
    API --> INT
    WI --> AUD
    IT --> AUD
    COL --> AUD
    WI --> NOT
    IT --> NOT
    COL --> NOT

    WI --> DB[("База данных")]
    IT --> DB
    COL --> DB
    AUD --> DB
    NOT --> DB

    API -->|проверка токена; схема открыта| IdP["Провайдер идентификации"]
    Tests["Сервис тестирования"] -->|REST, синхронно| API
    INT --> WI
    Reports["Сервис отчётности"] -->|REST, синхронный read-only запрос| API
    INT -->|REST, синхронно, по расписанию| Refs["Сервис справочников"]
```

Все внешние взаимодействия — синхронные REST-вызовы: сервис тестирования и сервис отчётности вызывают Task Tracker, а Task Tracker вызывает сервис справочников. Сервер проверяет токен и права, но конкретный IdP/способ проверки остаётся открытым. Аудит и уведомления записываются транзакционно внутри приложения; внешней шины сообщений нет (ADR-004, ADR-007).
