name: Reference Manager Release

on:
  push:
    branches:
      - main

    paths:
      - 'reference-manager/**'

jobs:
  dummy:
    runs-on: ubuntu-latest

    steps:
      - name: Dummy step
        run: echo "Dummy job for release changes in reference-manager"